"""Test suites.

For continuous testing of code (and tests) in a module, install `entr`
and run this.

$ ack --type=python bible_aligments tests/bible_aligments -f | entr -r poetry run pytest tests

"""
